pipeline { 
    agent any 
    stages { 
        stage('Checkout') { 
            steps { git url: 'https://github.com/zhangxsgit/jenkinsproject.git', branch: 'main' } 
        }
            
        stage('Build and Run') { 
            steps { sh 'python3 hello.py' } 
        }    
    } 
    
}
